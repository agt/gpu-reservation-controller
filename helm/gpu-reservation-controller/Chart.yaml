apiVersion: v2
name: gpu-reservation-controller
description: Kubernetes controller that enforces time-bound GPU reservations by patching pods with tolerations
type: application
version: 0.2.0
appVersion: "latest"
keywords:
  - gpu
  - reservation
  - controller
  - kubernetes
maintainers:
  # `name` is mandatory — `helm lint` fails the chart without it.
  - name: agt
    email: agt@ucsd.edu
