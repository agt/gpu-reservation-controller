apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "gpu-reservation-controller.fullname" . }}
  labels:
    {{- include "gpu-reservation-controller.labels" . | nindent 4 }}
rules:
  # The controller lists and watches pods across all namespaces to detect
  # reservation candidates, patches pods to apply tolerations and set
  # activeDeadlineSeconds, and deletes pods when a reservation is cancelled.
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch", "patch", "delete"]
  # emit_runtime_capped_event and emit_reservation_cancelled_event create
  # Normal events on admitted pods.
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["create"]
